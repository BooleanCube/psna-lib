namespace psna_lib;

public static class ID
{
    // Message IDs
    public const byte m_SUBSCRIBE_REQUEST = 0;
    public const byte m_UNSUBSCRIBE_REQUEST = 1;
    public const byte m_NETWORK_PUBLISH = 2;
    public const byte m_DIRECT_PUBLISH = 3;

    // Topic IDs
}